## Testing framework - Cypress (e2e)

- The first test logs into the web application, goes to the page under test, checks the functionality of adding fields, entering data into them (valid/invalid), checks the calculation of formulas and deletes the new indicator, the second test checks the output of error messages when entering invalid values.

- Some selectors and constants are rendered in consts.js and are imported in the test.
